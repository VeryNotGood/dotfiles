local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "elixir", "eex", "heez", "exs" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
